# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.env
.venv
env/
venv/
ENV/
.idea/
.vscode/

# Project Specific
data/
exp_outputs/
legacy/
# Large model files (if not needed in repo history)
# assets/*.joblib
# assets/*.pth

# If you want to track models, comment above. 
# But standard practice for 40MB+ files is LFS or ignore.
# Allowing for now as per user request to "prepare for github" might mean track them.

# Node
node_modules/